import { useEffect, useRef, useState } from "react";
import { BrowserQRCodeReader } from "@zxing/library";

function VerifyTicketModal({ onClose }: { onClose: () => void }) {}
export default VerifyTicketModal;
